You are an expert QA engineer and Playwright test automation specialist.

Your task is to generate exactly ONE Playwright end-to-end test in TypeScript for the given use case.

Important requirements:
- Output ONLY valid TypeScript code.
- Do NOT wrap the answer in markdown code fences.
- Use Playwright test syntax with:
  import { test, expect } from '@playwright/test';
- The test must be runnable as a `.spec.ts` file.
- Use the base URL: http://localhost:5173/open-pioneer-trails-weather-app/
- Prefer getByTestId() for elements listed in the locator context below.
- Only fall back to getByRole/getByText if no matching testid exists.
- Never invent testid values that are not listed in the context.
- Keep the test readable and well-structured with short step comments.
- Use expect assertions based on the expected_result field.
- Avoid unnecessary waits. Prefer Playwright auto-waiting.
- Use a single test() block.
- The test title should include the use case id and title.
- Do not test canvas or map rendering directly.
- Always start by navigating to the app using `await page.goto('http://localhost:5173/open-pioneer-trails-weather-app/');`.
- Return only the final TypeScript code.


The following locators and UI structure were extracted from the live application.
Use these as your primary source for selectors — do not invent selectors that are not listed here.

data-testid attributes found in the app:
  - app
  - bottom-left-measurement-anchor
  - bottom-left-tools-anchor
  - coordinate-viewer
  - footer
  - footer-flex
  - header
  - header-title
  - info-panel
  - initial-extent-button
  - legend-toggle
  - location-empty
  - location-viewer-heading
  - map-container
  - map-controls-panel
  - map-root
  - map-tools
  - measurement-toggle
  - scale-bar
  - scale-viewer
  - toc-panel
  - toc-toggle
  - top-left-anchor
  - top-right-anchor
  - use-cases-button
  - weather-forecast-heading
  - weather-forecast-separator
  - weather-forecast-toggle
  - weathercard-empty
  - zoom-in-button
  - zoom-out-button


Use case:
{
  "id": 10,
  "title": "Measure a distance on the map",
  "description": "The user wants to activate the measurement tool and measure a distance directly on the map.",
  "preconditions": [
    "The user has started the app.",
    "The map is visible."
  ],
  "steps": [
    "The user clicks the measurement tool button.",
    "The app activates the measurement mode and opens the measurement panel.",
    "The user interacts with the map to define a measurement line.",
    "The user ends the measurement line with a double click.",
    "The app calculates and displays the measured distance."
  ],
  "expected_result": [
    "Measurement mode is activated.",
    "The measurement panel is visible.",
    "A measurable geometry can be drawn on the map.",
    "The measured result is displayed to the user."
  ],
  "complexity": "medium"
}

Now generate the Playwright TypeScript test file content only.